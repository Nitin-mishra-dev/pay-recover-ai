# Benchmark Workflow

Run the complete PayRecover evaluation.

Use fixed seed.

Run:

1. No Action
2. Blind Retry
3. Static Rules
4. PayRecover

Verify independent outcome generation.

Calculate:

- total at-risk revenue
- revenue recovered
- incremental revenue
- uplift
- action cost
- net value
- unnecessary interventions
- escalation
- safety violations

Recompute headline metrics independently.

Write:
eval/results/
eval/report/

Then verify README metrics match generated results.

Never invent or manually alter benchmark numbers.
